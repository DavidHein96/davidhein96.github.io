---
title: "InstaWell: Open-Source Thermal Shift Assay Analysis in Python"
author: "Dave Hein"
date: "2025-06-01"
categories: [Python, Open Source Software]
image: "instawell_cover.jpg"
---

## How This Started

```{=html}
<!-- DRAFT: HIGH PRIORITY — this is the most fabricated section across all posts.
     I invented the entire origin story. Key things to replace with reality:
     - "watching a collaborator spend an entire afternoon" — did this happen? Who was it?
       Or did the project start some completely different way?
     - "I'd been working in clinical data science" — this framing is true but was it
       actually what you were thinking at the time?
     - "quick script to help that one collaborator" — is this the real origin or not?
     - "every lab doing thermal shift assays has their own ad-hoc Excel workflow" —
       is this actually true from your I-Corps customer discovery, or am I assuming?
     - The air bubble detail is me trying to sound like I know benchwork. Cut if fake.
-->
```

This project was born from watching a collaborator spend an entire afternoon copying fluorescence readings from a plate reader export into Excel, manually calculating derivatives, eyeballing melting temperatures, and then doing it all again for the next plate. I'd been working in clinical data science — building pipelines that process thousands of records automatically — and it was jarring to see bench scientists still doing this by hand in 2024.

The tool started as a quick script to help that one collaborator, then grew as I realized the problem was universal: every lab doing thermal shift assays has their own ad-hoc Excel workflow, and none of them are reproducible. Two people analyzing the same plate can get different Tm values depending on how they smooth the curves, where they draw the baseline, and whether they caught that one well that had an air bubble.

```{=html}
<!-- DRAFT: The I-Corps paragraph — verify:
     - "talking to 30+ potential users" — is that the right number?
     - "simplicity for non-coders, full traceability, messy plate layouts" —
       are these the actual priorities that came out of customer discovery?
     - Was InstaWell the focus of I-Corps, or was it a different tool/suite?
     - What was the most surprising thing you learned during I-Corps?
-->
```

InstaWell is also connected to our team's NIH I-Corps experience — I led a team through the entrepreneurial training program focused on our lab's suite of scientific tools, and the process of customer discovery (talking to 30+ potential users about their pain points) directly shaped InstaWell's design priorities: simplicity for non-coders, full traceability for publications, and the ability to handle messy real-world plate layouts.

## What It Does

[**InstaWell**](https://github.com/DavidHein96/InstaWell) is an open-source Python package that automates the analysis of thermal shift assay (TSA/DSF) data. Thermal shift assays are a workhorse technique in drug discovery and protein biochemistry — they measure how a protein's thermal stability changes in the presence of ligands or buffer conditions. The raw instrument output is just a big matrix of fluorescence readings across dozens of wells and temperatures.

```{=html}
<!-- DRAFT: Is this Excel workflow description accurate? I'm guessing based on general knowledge
     of how people process plate reader data. If you've seen the actual Excel workflows,
     describe what they really look like. "an hour" — is that right?
-->
```

The manual Excel approach looks something like: export CSV from instrument, copy into a template, manually subtract background wells, normalize each trace by hand, compute numerical derivatives in a separate column, scan for the minimum by eye, and paste the result into a summary table. For a 96-well plate with replicates, that's an hour of tedious, error-prone work — and it leaves no audit trail.

InstaWell replaces that with a reproducible, auditable pipeline that goes from raw instrument files to publication-quality figures and clean CSV summaries.

[Documentation](https://davidhein96.github.io/InstaWell/) \| [GitHub](https://github.com/DavidHein96/InstaWell) \| [PyPI](https://pypi.org/project/instawell/)

## Installation

``` bash
pip install instawell              # core package
pip install 'instawell[notebook]'  # + Jupyter widget support
pip install 'instawell[dash]'      # + web UI
```

## The Pipeline

InstaWell follows an 8-step sequential pipeline, generating numbered CSV files at each stage for full traceability:

1.  **Raw organization** — wide-to-long transformation with flexible condition parsing from your plate layout
2.  **Well filtering** — exclude problematic wells
3.  **Replicate averaging** — group by unique conditions and compute means
4.  **Background subtraction** — NPC-aware signal correction
5.  **Min-max scaling** — normalize traces to \[0, 1\] for comparison
6.  **Derivative computation** — rate-of-change analysis across temperatures
7.  **Tm extraction** — identify minimum derivative temperatures
8.  **Curve fitting** — 4-parameter logistic (4PL) regression in log10 dose space

Every intermediate result is saved as a numbered CSV, so you can inspect or branch off at any point.

## Flexible Condition Parsing

One of the design goals was supporting arbitrary experimental layouts. Your plate layout file maps wells to any combination of condition fields — concentration, ligand, protein, buffer, etc. — using a configurable delimiter:

```         
well_row,1,2,3
A, 0|DMSO|NPC|PBS, 1.5|DrugX|ProteinA|PBS, 6|DrugX|ProteinA|PBS
```

The `ExperimentContext` class manages all pipeline settings including field names, delimiters, NPC markers, and the temperature column.

## Three Ways to Use It

### Python API

The core pipeline is designed for scripting and Jupyter notebooks. Each step is a function call that reads the previous CSV and writes the next:

``` python
from instawell import ExperimentContext

ctx = ExperimentContext(
    condition_fields=("concentration", "ligand", "protein", "buffer"),
    condition_separator="|",
    non_protein_control_marker="NPC",
)
```

### Jupyter Widgets

Built-in widget functions let you interactively browse generated figures directly in notebooks:

-   `raw_figures_widget()` — browse raw per-well traces
-   `processed_figures_widget()` — view normalized and derivative curves
-   `min_temp_figures_widget()` — inspect Tm extraction results

### Dash Web App

For users who prefer a GUI, InstaWell includes a full Dash web interface for uploading data, designing plate layouts, running the pipeline, and browsing interactive Plotly figures — no coding required:

``` bash
instawell-dash --port 8050 --experiments-root experiments
```

## Tech Stack

-   **pandas / NumPy** for data manipulation
-   **SciPy** for 4PL curve fitting
-   **Plotly** for interactive visualizations
-   **Dash** for the optional web UI
-   **MkDocs** for documentation
-   **pytest** for the test suite (unit, integration, fuzz testing)
-   **uv** for package management

## What's Next

```{=html}
<!-- DRAFT: I made up the planned features. Replace with your actual roadmap:
     - "currently optimized for Bio-Rad and generic CSV" — is that right?
     - "multi-plate experiments" and "dose-response fitting" — are these real plans?
     - Is it actually in beta? What's the real status?
     - Has anyone outside your team actually used it yet? If so, what happened?
     - The "I'd love to hear what breaks" call to action is fine if you mean it.
-->
```

InstaWell is currently in beta — the core pipeline is stable and tested, but I'm actively looking for early adopters to stress-test it with different instruments and experimental designs. If your lab does thermal shift assays and you're tired of Excel, I'd love to hear what breaks. File an issue on GitHub or reach out directly.

On the development side, the next features are: support for additional instrument export formats (currently optimized for Bio-Rad and generic CSV), multi-plate experiments with automatic cross-plate normalization, and a dose-response fitting module that generates IC50/EC50 curves directly from the Tm shifts.

## Links

-   [Full documentation](https://davidhein96.github.io/InstaWell/)
-   [GitHub repository](https://github.com/DavidHein96/InstaWell)
-   [PyPI package](https://pypi.org/project/instawell/)