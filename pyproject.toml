[project]
name = "davidhein96-github-io"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
]

[project.optional-dependencies]
agentic-llm-workflows = [
    "openai>=2.26.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.5",
    "ty>=0.0.20",
]


# pyproject.toml
[tool.ruff]
extend-include = ["*.mdx"]

[tool.ruff.format]
preview = true
docstring-code-format = true
